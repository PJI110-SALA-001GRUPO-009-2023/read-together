<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img class="" id="logo" src="/midia/imagens/Logos/logo-rt-h.png" alt="Logo H Read-Together">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0 ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="">Meu Clube</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/usuario/editar">Perfil</a>
                </li>
                <li class="nav-item">
                    <button class="nav-link btn btn-link" data-bs-toggle="modal"
                    data-bs-target="#logoutModal">Sair</button>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="modal fade" id="logoutModal" tabindex="-1" aria-labelledby="logoutModalLabel" aria-hidden="true">
    <div class="modal-md modal-dialog modal-dialog-scrollable">
        <div id="modal-conteudo" class="modal-content">
            <div class="modal-header">
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div id="corpo-modal" class="modal-body m-auto">
                <p class="fs-5">Tem certeza que deseja deslogar?</p>
            </div>
            <div class="modal-footer">
                <button id="close-modal" type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    Cancelar
                </button>
                <button type="button" class="btn btn-primary" id="logout-button">Sim</button>
            </div>
        </div>
    </div>
</div>

<script>
    document.querySelector("#logout-button").addEventListener('click', async (e) => {
        const res = await fetch('/logout', {
            method: 'POST',
        })
        if (res.redirected) {
            window.location = res.url
        }
    })
</script>